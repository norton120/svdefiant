x-hugo: &hugo
  image: svdefiant-dev
  build:
    context: .
    dockerfile: Dockerfile
  volumes:
    - .:/src

services:
  blog:
    <<: *hugo
    ports:
      - "1313:1313"
    command: serve

  new-post:
    <<: *hugo
    profiles:
      - tools
    entrypoint: /bin/sh ./startup.sh new-post
